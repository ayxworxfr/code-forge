import request from '@/utils/request';
import type { {{ table.shortClassName }}, {{ table.shortClassName }}Query, {{ table.shortClassName }}Form, PageResult } from './types';

/**
 * {{ table.tableComment or table.tableName }} API
 * 
 * @author {{ global.author }}
 * @date {{ global.date }}
 */

/**
 * 分页查询{{ table.tableComment or table.tableName }}
 */
export function page{{ table.shortClassName }}(params: {{ table.shortClassName }}Query) {
  return request.get<PageResult<{{ table.shortClassName }}>>('/{{ global.moduleName }}/{{ table.shortCamelName }}/page', { params });
}

/**
 * 查询{{ table.tableComment or table.tableName }}列表
 */
export function list{{ table.shortClassName }}(params?: {{ table.shortClassName }}Query) {
  return request.get<{{ table.shortClassName }}[]>('/{{ global.moduleName }}/{{ table.shortCamelName }}/list', { params });
}

/**
 * 根据ID查询{{ table.tableComment or table.tableName }}
 */
export function get{{ table.shortClassName }}(id: number | string) {
  return request.get<{{ table.shortClassName }}>(`/{{ global.moduleName }}/{{ table.shortCamelName }}/${id}`);
}

/**
 * 新增{{ table.tableComment or table.tableName }}
 */
export function create{{ table.shortClassName }}(data: {{ table.shortClassName }}Form) {
  return request.post<boolean>('/{{ global.moduleName }}/{{ table.shortCamelName }}', data);
}

/**
 * 修改{{ table.tableComment or table.tableName }}
 */
export function update{{ table.shortClassName }}(data: {{ table.shortClassName }}Form) {
  return request.put<boolean>('/{{ global.moduleName }}/{{ table.shortCamelName }}', data);
}

/**
 * 删除{{ table.tableComment or table.tableName }}
 */
export function delete{{ table.shortClassName }}(id: number | string) {
  return request.delete<boolean>(`/{{ global.moduleName }}/{{ table.shortCamelName }}/${id}`);
}
